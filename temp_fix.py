# Geçici fix dosyası - satırları kopyala
    # SOL PANEL: Ürün Listesi (genişlik artırıldı)
    left_panel = tk.Frame(middle_section, bg=CARD_COLOR, width=750)
    left_panel.pack(side="left", fill="both", expand=True, padx=(0,8), pady=0)
